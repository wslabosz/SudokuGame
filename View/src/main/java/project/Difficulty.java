package project;

import java.util.Arrays;
import java.util.List;

public class Difficulty {
    List<Integer> coordinates = Arrays.asList(0, 1, 2, 3, 4, 5, 6, 7, 8);

    private void fillRandomPositionsList(int capacity) {
        for (int i = 0; i < capacity; i++) {

        }
    }
}
